
class Settings {
    public static var displayedNumberCount : Int = 5;     // Сколько чисел показывается
    public static var numberDigitCount : Int = 3;         // Сколько разрядов у одного числа
    public static var numberDisplayTime : Float = 1.0;    // Время показа одного числа (в секундах)
    public static var answerTimeLimit : Float = 5.0;      // Время на подсчет суммы всех чисел (в секундах)
}
